# SMTP Configuration
SMTP_HOST=smtp.example.com
# SMTP_PORT: Use 587 for STARTTLS or 465 for SSL/TLS
# Port 587 (STARTTLS) - starts unencrypted, upgrades to TLS (recommended)
# Port 465 (SSL/TLS) - encrypted from the start
SMTP_PORT=587
SMTP_USER=your-email@example.com
# SMTP_PASSWORD: Use app-specific password for Gmail/Outlook
# Gmail: https://myaccount.google.com/apppasswords
# Outlook: Account settings → Security → App passwords
SMTP_PASSWORD=your-password

# Email Configuration
MAIL_TO_DESTINATION=destination@example.com
SENDER_NAME=SISU Simulator Contact Form

# Application Port (optional, defaults to 3000)
PORT=3000
